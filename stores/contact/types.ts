export type createContactStatus = {
    status: boolean;
    msg: string;
}

export type contacts = {
    contact_id: number;
    name: string;
    email: string;
    phone: string;
    message: string;
    created_at: string;
}